// eslint-disable-next-line @typescript-eslint/ban-ts-comment
// @ts-nocheck
import React from "react";
import { Card, CardHeader, CardTitle, CardDescription, CardContent } from "@allyson/ui/card";
import { useUser } from "@allyson/context";

// create a component in here so if the user is on the business plan then display their orgnaniztion if they are the admin they can make changed and add people if not they can jsut see what organization they are apart of the admin can send invites to invite team members in and then they can also 
export default function OrganizationComponent() {
  return <div>Organization</div>;
}
